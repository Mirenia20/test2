//using Raccoon;
/*
int main(string[] args) {
    Test.init(ref args);

    Test.add_func("/utils/format_file_size/bytes", () => {
        assert(Utils.format_file_size(500.0, form.FORM_XB) == "500.00 B");
    });

    Test.add_func("/utils/format_file_size/kibibytes", () => {
        assert(Utils.format_file_size(2048.0, form.FORM_XIB) == "2.00 KiB");
    });

    Test.add_func("/utils/format_file_size/megabytes", () => {
        double size = 5 * 1000 * 1000; // 5 MB
        assert(Utils.format_file_size(size, form.FORM_XB) == "5.00 MB");
    });

    return Test.run();
}
*/
